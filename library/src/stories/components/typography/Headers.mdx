import React, { Fragment } from "react";
import { Meta, Typeset } from "@storybook/blocks";
import { theme } from "../../../theme/theme";
import { valueToRem, remToPx, convertToNumber } from "../../../theme/theme.utils";
import { DUMMY_MESSAGE } from '#/utility/const';

<Meta title="Components/Typography/All Headings" />

{Object.entries(theme.fonts.weights).map(([weightType, weight]) => (
   <Fragment key={weightType}>
     <h3>
       Weight: {weightType} ({weight})
     </h3>
     <Typeset
       fontSizes={Object.values(theme.components.typography.head).map((value) => remToPx(value.fontSize))}
       fontWeight={weight}
       sampleText={DUMMY_MESSAGE}
       fontFamily={theme.fonts.defaultFamily}
     />
   </Fragment>
 ))}
